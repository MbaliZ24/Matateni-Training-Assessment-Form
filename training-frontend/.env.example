# Optional: set when the API is on a different host (e.g. production builds).
# Leave unset for local dev — Vite proxies /api to http://localhost:5000 (see vite.config.ts).
VITE_API_URL=http://localhost:5000
jwt_secret=your_jwt_secret_key_here